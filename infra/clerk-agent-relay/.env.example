# The only secret this service needs. Put it in your secrets manager, not here.
ANTHROPIC_API_KEY=sk-ant-...

# Local dev server only. 8787 is the vision proxy's default and 8788 is what
# `npm run vision:proxy` already takes, so the relay starts one along.
PORT=8789
