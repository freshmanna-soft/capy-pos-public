{
  "name": "capy-infra-shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Code shared by the infra/ services. Today: the DocumentStore persistence port, its in-memory reference implementation, and the conformance suite every implementation has to pass.",
  "scripts": {
    "test": "node --experimental-strip-types --test \"src/**/*.test.mjs\""
  }
}
