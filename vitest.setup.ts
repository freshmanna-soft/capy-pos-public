import { beforeAll, afterAll, afterEach } from 'vitest';

beforeAll(() => {
  // Global setup
});

afterEach(() => {
  // Cleanup after each test
});

afterAll(() => {
  // Global teardown
});

// Made with Bob
